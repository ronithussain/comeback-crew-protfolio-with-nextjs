/**
 * description: add to cart feature with redux tool kit and toast notification (4 steps)
 * 
 * step 1: set up redux tool kit (3 tasks);
 * task 1: create the cart slice
 * task 2: create the redux store
 * task 3: wrap the app with the redux provider
 * 
 * step 2: display cart items count in the navbar (2 tasks)
 * task 1: update the navbar component to show item count.
 * task 2: update the ProductCart to trigger cart updates
 * 
 * step 3: add toast notification (2 tasks)
 * task 1: add <Toaster/> component to layout
 * task 2: trigger toast in the ProductCart component
 * 
 * step 4: display selected items in the cart (2 tasks)
 * task 1: update ProductCard to support item display
 * task 2: build the cartPage
 */




/**
 * Wishlist: add to wishlist feature with redux toolkit and toast notification(4-steps)
 * 
 * step-1: update redux toolkit (2- tasks)
 * task-1:create the wishlist slice.
 * task-2: update the redux store.
 * 
 * step-2: display wishlist items count in the navbar(2 tasks)
 * task-1: update the navbar component to show item count
 * task-2: update the productCard to trigger wishlist updates
 * 
 * step-3: add toast notification.
 * 
 * step-4: display the selected items in the wishlist page
 */